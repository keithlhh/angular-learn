import { SexChangePipe } from './sex-change.pipe';

describe('SexChangePipe', () => {
  it('create an instance', () => {
    const pipe = new SexChangePipe();
    expect(pipe).toBeTruthy();
  });
});
