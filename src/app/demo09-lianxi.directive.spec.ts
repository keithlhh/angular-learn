import { Demo09LianxiDirective } from './demo09-lianxi.directive';

describe('Demo09LianxiDirective', () => {
  it('should create an instance', () => {
    const directive = new Demo09LianxiDirective();
    expect(directive).toBeTruthy();
  });
});
